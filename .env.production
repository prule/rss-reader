# Production build config for Cloudflare Pages.
# Vite loads this automatically during `pnpm build` (mode=production).
# The deployed feed relay Worker the app calls to fetch feed XML.
VITE_RELAY_URL=https://rss-reader-pwa-relay.paulrule1.workers.dev
