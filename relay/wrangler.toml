# Cloudflare Worker config for the Ferrite feed relay.
# Local dev: `pnpm run relay:dev` (serves on http://localhost:8787), which the
# Vite dev server proxies at /relay. Deploy: `wrangler deploy`.
name = "ferrite-relay"
main = "worker.ts"
compatibility_date = "2024-11-01"

# The relay is stateless — no KV, D1, R2, or Durable Object bindings.
